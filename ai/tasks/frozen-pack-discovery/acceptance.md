# Acceptance criteria

The frozen desktop server starts in demo mode, reports the Microsoft Admin capability in `/packs/status`, marks its router mounted, and continues enforcing the normal pack manifest, route dependency, and product safety boundaries.
