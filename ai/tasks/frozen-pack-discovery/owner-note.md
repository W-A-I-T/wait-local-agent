# Owner-facing reason

This is product correctness, not a cosmetic CI fix: without reliable frozen discovery, source installs can expose Microsoft Admin while the downloadable desktop sidecar silently omits the capability at runtime.
