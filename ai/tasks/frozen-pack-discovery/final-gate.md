# Final gate

Do not merge based only on the unit regression test. The built PyInstaller sidecar must pass the existing live startup check and return a mounted Microsoft Admin router from `/packs/status`.
