# CI linkage

This branch responds to the `main` workflow failure introduced when the repaired sidecar verification gate first exercised the newly merged Microsoft Admin pack. The failure occurred after the sidecar binary built successfully and specifically at `Verify capability packs in the sidecar`.
