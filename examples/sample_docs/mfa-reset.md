# MFA Reset Runbook

Use identity-provider admin tools to verify the requester, confirm backup contact information, revoke stale sessions when appropriate, and require the user to register the new device.

Do not disable MFA permanently. Escalate if identity proofing is incomplete.

