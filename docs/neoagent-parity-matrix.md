# NeoAgent parity matrix

This is a clean-room capability matrix. It compares publicly documented
capability classes with the behavior verified in this repository; it is not a
claim of source, API, wording, or implementation compatibility. The public
NeoAgent documentation is used only as a feature-class reference.

| Capability class | WAIT already has | Partial | Missing | Smallest next implementation |
| --- | --- | --- | --- | --- |
| Bounded agent runtime | Explicit tool allowlists, eight-step cap, timeout, approval pause, active-run cancellation, redaction, tenant scope, persisted runs, selectable ticket/client/local-knowledge context, and redacted operational final results | Event context now reaches the same executor | Unrestricted planning and chat | Add richer context policies only when an existing source can satisfy them |
| Bounded planning | `POST /agents/plan` and React Agents control select a bounded sequence of existing catalog tools using the configured model provider when available or deterministic rules otherwise; model output is JSON-parsed, catalog-allowlisted, bounded, redacted for remote providers, and falls back safely; plans include tenant-scoped context, expose selection/risk/access/approval metadata without executing, and can create a disabled draft definition for review | Result-aware continuation is opt-in on reviewed definitions; after each successful step it selects one remaining approved tool from the same catalog, records the operational selection mode, and falls back to the reviewed sequence when the provider is unavailable or invalid | Richer reviewed plan generation and broader result-aware policy composition | Extend the existing provider and agent-definition contracts without creating a second runtime |
| Tool catalog | Existing smart actions plus read-only local knowledge search, tenant-scoped documentation-assisted response drafting, ticket-quality checks, collector previews, HaloPSA ticket lookup and approval-gated ticket-write tools, ConnectWise PSA ticket lookup and approval-gated status, assignment, and ticket-field writes, Syncro, ServiceNow incident lookup plus approval-gated work-note/state/assignment/resolution-metadata updates, and Autotask ticket lookup plus approval-gated ticket-note, time-entry, status, resolution, and assignment updates, Hudu, IT Glue, Confluence, Notion, and SharePoint documentation search, tenant-mapped TimeZest scheduling-request reads and approval-gated documented create, separately mapped ScalePad Core client, ControlMap risk-summary, and Lifecycle Manager goal/assessment reads, approval-gated Notion page comments, RMM operations, communication previews, and approval-gated Microsoft 365 onboarding, offboarding, group-membership, direct-license, session-revocation, mailbox-settings, message-move, message-read-state, message-delete, and managed-device action tools exposed with schemas, risk, role, approval, and access mode | Collector run/confirmation remains an explicit collector API; broader connector coverage is incomplete; TimeZest reschedule/cancel and ScalePad writes/unscoped reads remain unavailable | Complete connector tool coverage | Wrap one existing capability at a time |
| Agent definitions | SQLite definitions with enable/disable, trigger, filters, tools, steps, scope, selectable context sources, bounded per-agent approval deadlines, additive conditional approval rules for explicit ticket priority/status/requester-role matches, opt-in result-aware continuation, version counter, immutable revision snapshots, redacted diffs, restore-as-new-version, run-to-version links, same-tenant dependencies, and optional local execution windows; API and React agent builder support create/edit, history, compare, restore, and conditional rule editing | `manual`, `scheduled`, and `event` triggers; windows use validated `HH:MM` bounds and IANA timezones, including overnight ranges; conditional rules are exact case-insensitive matches and all entered fields are ANDed; authenticated manual/API runs can match `end_user`, `viewer`, `technician`, or `admin`, while scheduled/event/legacy runs have no requester role and do not match role conditions | Conversational/unrestricted planning and richer policy composition beyond explicit ticket fields | Extend the existing additive policy contract only with stricter, testable conditions |
| Event triggers | Authenticated ticket event ingestion, deterministic filters, idempotency, audit, run-once-per-entity, per-agent attempt state, a bounded retry queue with per-delivery `max_retries` (0-10) and `retry_delay_seconds` (1-3600), and Overview visibility with technician/admin manual retry | Supported event vocabulary is intentionally small; retries remain local and batches remain small; ingestion remains API/webhook-only | Vendor-native webhook adapters and richer trigger policy | Add source validation and further policy only with explicit safety bounds |
| Scheduled workflows | SQLite + APScheduler, cron, interval, and one-time schedules, pause/resume/reschedule/delete, workflow and agent targets, bounded client-scoped QBR, automation-opportunity, and recurring-service-review report targets, validated IANA schedule timezones, and closed agent windows skipped and audited, plus completion events for scheduled workflow and agent runs | Completion events remain compact and local; scheduled reports create deterministic local reports and audit success or failure | Richer schedule-triggered policies | Extend completion events only when an existing bounded event source can satisfy them |
| Dependencies/chaining | Event agents persist dependency IDs, reject missing/cross-tenant/cyclic relations, wait for upstream completion in a bounded sequential pass, and receive automatic tenant-scoped `workflow.completed` events from CLI/API/gallery and scheduled workflow/agent runs | Fan-out/fan-in remains intentionally small | Richer workflow-finished policies and visualization | Extend completion events to other run entry points without creating a second engine |
| Templates | Fixed public workflow catalog with ticket triage/quality/sentiment/security/SLA/stale/dispatch/recurring-service-review playbooks plus approval-gated Microsoft 365 onboarding, offboarding, password-reset, authentication-method-removal, and license-request playbooks; editable agent definitions; and tenant-scoped gallery instances with editable metadata, enable/disable, revision history, restore-as-new-version, validated versioned JSON import/export, API, CLI, and React UI paths | No marketplace distribution; imports remain disabled until reviewed and gallery behavior remains bounded to reviewed source templates; authentication-method removal remains explicitly ID-scoped and broad MFA reset is not exposed | Pack-backed gallery templates and review lifecycle | Add only further bounded password/MFA composition behind documented local/provider contracts |
| MSP intelligence | Triage, summary, resolution suggestion, similarity, dispatch suggestion, read-only knowledge search, tenant-scoped documentation-assisted response drafting with local/provider fallback and citations, ticket-quality, deterministic sentiment, escalation, security-alert, L1-resolution, and duplicate-ticket assessments, explicit-threshold SLA-risk assessment and stale-ticket sweep templates with bounded API/CLI payloads, tenant-scoped stale-ticket sweeps, approval-gated inactive-ticket follow-up and P1 alert actions through the shared communication action, recurring-service-review reports and workflow primitives, and approval-gated Microsoft 365 onboarding/offboarding/password-reset/authentication-method-removal/license-request playbooks through the shared tool runtime | Ticket merge is not a unified tool; duplicate-ticket review only identifies candidates and never merges or closes; security-alert detection is bounded lexical evidence and never takes side effects; documentation-assisted response stops with an explicit `no_relevant_sources` or provider-unavailable result when evidence cannot be gathered; SLA, stale, and recurring-service-review results require explicit local timestamps and operator thresholds; recurring service review reports still identify follow-up candidates as review outputs, while follow-up, documentation-assisted response, and P1 alert workflows can execute only after approval; external delivery requires a configured adapter; password reset requires a valid local vault reference, authentication-method removal requires an explicit method type and immutable method ID, onboarding requires a valid local vault reference, offboarding requires both explicit user identity and directory ID, and license requests require immutable SKU IDs | Broader MSP action catalog | Add further governed M365 mutations one at a time before adding models |
| Human approval | Preview, edit, approve/reject, approver identity, bounded 24-hour expiry, bounded per-agent deadline shortening, persisted additional approval rules for selected enabled tools, bounded conditional rules for explicit ticket priority/status/requester role, audited expiration, linked-run termination, gated HaloPSA writes, audit | Rules remain additive and agent-scoped; connector-specific condition composition is incomplete | General policy composition beyond ticket fields and requester role | Extend the existing additive policy contract only with stricter, testable conditions |
| PSA | HaloPSA reads, a tenant-scoped HaloPSA ticket tool, approval-gated writes, ConnectWise PSA ticket/company reads plus approval-gated allowlisted ticket updates, Syncro ticket/customer reads plus documented paginated ticket-comment history and the approval-gated documented ticket-comment action, ServiceNow incident/company reads plus approval-gated work-note/state/assignment/resolution-metadata updates, and Autotask ticket/company reads plus approval-gated ticket-note, time-entry, status, resolution, and assignment updates | Deeper writes for other PSA providers and richer Syncro/ConnectWise/ServiceNow/Autotask action parity remain incomplete | Broader PSA write parity | Add one bounded write surface at a time |
| TimeZest scheduling | Tenant-mapped scheduling-request status and bounded appointment metadata plus approval-gated create through the shared tool catalog, API, CLI, agent definitions, and React Agents input editor | Create requires the global HTTP/write flags, a read/write provider key, explicit mapped company, and a second technician/admin approval; reschedule and cancel remain unavailable; broader marketplace coverage is incomplete | Provider-backed reschedule/cancel mutations and broader marketplace coverage | Add one documented mutation at a time only when the provider contract exists |
| ScalePad marketplace | Tenant-mapped, read-only Core client lookup, separately mapped ControlMap client risk-summary reads, and separately mapped Lifecycle Manager goal and assessment reads with documented filters and cursor pagination through the shared tool catalog, API, CLI, and dedicated connector routes | Writes, unscoped reads, and other ScalePad product APIs are unavailable; each surface requires its own explicit provider-ID map because Core, ControlMap, and Lifecycle Manager identifiers are not assumed equivalent; Lifecycle Manager may require a higher provider subscription | Broader ScalePad Core, Lifecycle Manager, ControlMap, BackupRadar, and Quoter coverage | Add one documented, tenant-scoped read surface at a time |
| RMM | Local endpoint collectors plus tenant-scoped device lookup, alerts, script catalog, script preview, and approval-aware execution; NinjaOne Public API adapter with explicit WAIT-to-organization mapping; Datto RMM devices, open alerts, component metadata, approval-gated quick jobs, and bounded job-status lookup with explicit WAIT-to-site mapping; N-able N-central tenant-scoped devices, active issues, and scheduled-task metadata plus bounded documented direct-task submission/status through an explicit organization-unit map; N-able N-sight documented XML site/server/workstation inventory, mapped-device check, performance-history, asset-details, and monitoring-details inventory, tenant-scoped failing-check, managed-antivirus threat, mapped-device outage, Backup & Recovery session, and 60-day backup-check history reads, mapped-device patch reads, approval-gated patch approval/reprocessing, and allowlisted patch policy operations through an explicit client map; Kaseya VSA X documented v3 organization-scoped device and device-notification reads, script catalog/detail, approval-gated script execution, and locally scoped execution polling with explicit client mapping; ScreenConnect RESTful API Manager session/device reads, approval-gated `AddNoteToSession` and `SendMessageToSession`, plus an optional local command catalog and approval-gated `SendCommandToSession` submission with an explicit local client-to-session UUID map | Broader RMM vendors, richer inventory, and provider-native remediation remain incomplete; N-sight script discovery, preview, execution, antivirus writes/scans, backup mutations, and other patch-management operations are unavailable; ScreenConnect provider-native alerts, script discovery, and command polling are unavailable; local catalog commands accept no runtime arguments and report provider acceptance rather than completion; Kaseya broader remediation, webhook callbacks, and script-source execution remain unavailable; N-central does not upload scripts, accept credentials/customer scope in payloads, or expose broader task management | ConnectWise RMM/Automate/Asio, broader N-sight patch/AV/backup remediation, ScreenConnect provider-native remediation, Kaseya broader remediation, and richer N-central remediation | Add one reviewed adapter or bounded capability behind the same contract |
| Documentation | Local knowledge ingestion/search, Hudu reads with bounded article content extraction and tenant-scoped content search, organization-scoped IT Glue document-name/text/step-content search with bounded document-detail section retrieval, Confluence page body retrieval and tenant-scoped content search, Notion mapped-page title search, bounded page-markdown retrieval, mapped data-source schema retrieval, mapped data-source row queries, and approval-gated page comments, and read-only SharePoint metadata plus Graph site/folder content search and bounded text-document content retrieval | IT Glue and SharePoint searches are capped at 50 provider results per request; Notion title search uses the explicit local client-page map and data-source reads use a separate local client-data-source map; schema responses expose only bounded property names/types and query bodies are fixed; page comments use the documented comments endpoint only after approval and write opt-in; SharePoint binary/office extraction is not exposed | Broader documentation search, Notion page/property writes and other comment capabilities, provider-native filters and richer content extraction | Add one read-only adapter behind existing provider patterns |
| Microsoft 365 | Tenant-scoped collected-inventory lookup plus bounded Graph user, group, subscribed-license, per-user license-detail, mailbox-folder, metadata-only message, and Intune managed-device reads; approved user creation, disable/offboarding, password reset from a local-vault reference, explicit authentication-method removal, strict-ID group membership add/remove, strict-ID direct user license add/remove, approved session revocation, approved Intune managed-device sync/reboot/retirement and remote-lock, allowlisted mailbox-settings updates, message move, message read-state, and message delete updates; the shared tool catalog exposes the same admin-only, approval-gated operations | Broader device resources and other message/device operations remain dedicated approval APIs; no reset-all MFA operation or live Graph integration is claimed without the configured contract and tests | Broader message actions and Intune operations | Add one governed M365 mutation at a time with approval |
| Communication | Preview and approval-gated delivery paths for local ticket notes, email, Teams, Slack, and SMS; documentation-assisted responses, inactive-ticket follow-ups, and P1 alerts reuse the same approval-gated communication action; external adapters use explicit local configuration; successful sends return and persist an opaque local receipt ID, UTC acceptance time, bounded adapter status, and webhook HTTP status when available | PSA-native ticket-note sync and provider-specific delivery features beyond the configured SMTP/webhook adapters; provider-issued receipt IDs and callbacks are not claimed | Broader PSA note synchronization and richer provider capabilities | Keep preview, approval, local receipt, and delivery as separate capabilities |
| Technician teammate | Run-now APIs, tool catalog, execution and approval history, technician-only `/technician/chat` and `technician-chat` CLI commands mapped to existing smart actions, explicit `plan ... TCK-*` previews reused from `/agents/plan`, persisted tenant/principal-scoped chat sessions with bounded follow-up ticket context and redacted history, a dedicated React `/technician-chat` session UI, a UI form for tenant-scoped Teams/Slack notification approval drafts, and explicit tenant-scoped RMM script preview/approval requests | Plan previews do not create or execute definitions; there is no external Teams conversation adapter or provider-specific notification history; script requests accept only explicit script/device IDs and still rely on the existing approval flow | Richer conversational context and approved action composition | Add only bounded context or an external channel adapter that reuses the same runtime |
| End-user support | Optional, separately tokenized end-user mode with requester/tenant-scoped local ticket creation, status lookup, isolated requester/support messages, technician escalation, a dedicated `/end-user` React surface, authenticated local branding name/tagline plus validated local logo/color configuration, operator API/React controls to review and add local support replies, and a manually initiated HaloPSA message-to-ticket approval draft after an explicit local-client mapping and remote ticket ownership check | Automatic PSA ticket synchronization, provider polling, and outbound end-user delivery remain unavailable; remote logo URLs are rejected; HaloPSA sync remains operator-triggered and approval-gated | White-label end-user agent with governed PSA/communication adapters | Add live adapters behind the same scope boundary |
| Backfills | `/agent-backfills` provides preflight ticket checks, 100-item execution with default sequential or persisted bounded parallelism, progress/count records, pause/cancel state, failed-item reruns, a non-persisting dry-run estimate at `/agent-backfills/preview`, and React dashboard controls | No connector-aware batch planning | Cross-connector batch planning and richer operator progress UI | Add connector-aware batch plans |
| Versioning | Agent `version` increments on update; gallery template versions persist redacted revisions, expose tenant-scoped revision diffs through the API, CLI, and React dashboard, restore as a new version, link gallery runs to the executed template version, and compare workflow runs through API, CLI, and React | Comparison is limited to persisted operational fields; provider metadata and full step-level comparison remain incomplete | Richer revision metadata and step-level run comparison | Extend existing revision views without versioning arbitrary application state |
| Observability | Runs, steps, artifacts, approvals, audit, redaction, analytics foundations, cancelled-run status, bounded retry lineage, per-agent event attempt state, React execution-history detail with technician artifact downloads, and safe provider/model metadata for smart-action runs | Trigger context is still compact | Richer provider metadata and per-step retry history | Extend existing execution records, not a new engine |
| Analytics | Local execution counts, outcomes, duration, estimated time saved, server-side date/client filters, explainable run-kind/trigger/status breakdowns, approval-rate aggregates, current ticket-resolution aggregates, explicit tenant-scoped local/imported ticket status history, recorded historical resolution counts/durations, per-workflow/action activity views, and a React analytics screen | No external-provider lifecycle analytics; existing imported snapshots do not provide inferred historical transitions | Richer provider analytics and connector-aware lifecycle ingestion | Extend existing lifecycle records only when a provider supplies explicit transition evidence |
| QBR and automation insights | Deterministic client-scoped `POST /reports/qbr`, `/reports/automation-opportunity`, and `/reports/recurring-service-review`, matching `reports` CLI commands, `/reports` controls, scheduler targets through `POST /scheduled-jobs` or `reports schedule`, and local JSON/Markdown/PDF export; reports summarize local ticket/status/activity evidence and label declared time-saved values as estimates | Reports use local/imported records and do not claim provider SLA compliance, measured savings, customer sentiment, or contract health; recurring service review is a bounded operational posture/follow-up report; schedules accept bounded rolling or explicit periods; external-provider lifecycle ingestion remains unavailable | Broader provider-backed QBR data and richer ROI modeling | Add only explicit provider evidence and measured fields behind the existing report contract |
| Model providers | Deterministic local mode and optional local OpenAI-compatible inference with safe fallback; explicit remote fallback adapters for Anthropic Messages and documented OpenAI-compatible DeepSeek, Kimi, coding-model-compatible, or generic endpoints; explicit offline mode denies remote calls; admin-triggered provider readiness probes use the documented model-list contract without exposing secrets; provider-reported token usage is recorded in redacted execution metadata when supplied; operator-supplied input/output rates produce clearly labeled cost estimates in execution metadata and tenant-scoped analytics; completion, planning, and continuation requests use a fixed two-retry transient-failure budget and record retry counts | Health checks validate configured model availability but do not provide provider uptime/SLA claims; cost estimates require both operator-supplied rates and complete provider usage; retries cover only 408, 429, 5xx, and transport failures | Broader provider-native capabilities and provider-specific lifecycle/cost APIs | Extend the single provider boundary with mocked contracts and redacted operational metadata |
| API | FastAPI CRUD, run, cancel, retry, approval, execution, tools, schedules, event deliveries with bounded manual and automatic retry, configurable per-delivery retry policy, backfills, and version APIs | Trigger vocabulary and connector-specific surfaces remain intentionally bounded | Full parity API surface | Add only documented capabilities with tenant/RBAC and failure-path tests |
| Voice/phone | None by design | None | Voice adapter and caller validation | Defer until core agent and connector paths are stable |

The matrix intentionally keeps missing functionality visible. A row moves to
“already has” only when an operator can exercise it through the public API,
CLI, or dashboard and tests cover the relevant safety boundaries.
